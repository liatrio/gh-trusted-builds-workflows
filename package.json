{
  "name": "gh-trusted-builds-workflows",
  "version": "1.0.0",
  "description": "A collection of reusable GitHub Actions workflows used in a demo on automated governance.",
  "type": "module",
  "scripts": {
    "prettier": "prettier --write '**/*.js' package.json",
    "prettier-check": "prettier --check '**/*.js' package.json",
    "test": "NODE_CONFIG_DIR=test/config node --test test"
  },
  "private": true,
  "devDependencies": {
    "@octokit/auth-app": "6.0.4",
    "@octokit/rest": "20.0.2",
    "adm-zip": "0.5.16",
    "async": "3.2.5",
    "config": "3.3.11",
    "prettier": "3.2.5"
  }
}
