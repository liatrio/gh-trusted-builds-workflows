{
  "name": "gh-trusted-builds-workflows",
  "version": "1.0.0",
  "description": "A collection of reusable GitHub Actions workflows used in a demo on automated governance.",
  "scripts": {
    "prettier": "prettier --write '**/*.mjs' package.json",
    "prettier-check": "prettier --check '**/*.mjs' package.json",
    "test": "NODE_CONFIG_DIR=test/config node --test test"
  },
  "private": true,
  "devDependencies": {
    "@octokit/auth-app": "5.0.3",
    "@octokit/rest": "19.0.11",
    "adm-zip": "0.5.10",
    "config": "3.3.9",
    "prettier": "2.8.8"
  }
}
