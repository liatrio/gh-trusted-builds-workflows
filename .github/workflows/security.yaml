on: [push]

jobs:
  show-claims:
    permissions:
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: 'Get Token'
        run: |
          curl -v -H "Authorization: Bearer ${ACTIONS_RUNTIME_TOKEN}" "${ACTIONS_ID_TOKEN_REQUEST_URL}" -H "Accept: application/json; api-version=2.0" -H "Content-Type: application/json" -d "{}"
#          TOKEN=$(curl -H "Authorization: Bearer ${ACTIONS_RUNTIME_TOKEN}" "${ACTIONS_ID_TOKEN_REQUEST_URL}" -H "Accept: application/json; api-version=2.0" -H "Content-Type: application/json" -d "{}" | jq -r '.value')
#          jwtd() {
#              if [[ -x $(command -v jq) ]]; then
#                  jq -R 'split(".") | .[0],.[1] | @base64d | fromjson' <<< "${1}"
#                  echo "Signature: $(echo "${1}" | awk -F'.' '{print $3}')"
#              fi
#          }
#          jwtd $TOKEN
